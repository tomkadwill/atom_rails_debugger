require 'spec_helper'

describe AtomRailsDebugger do
  it 'has a version number' do
    expect(AtomRailsDebugger::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
